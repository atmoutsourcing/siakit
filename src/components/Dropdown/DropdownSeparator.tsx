import { Flex } from '../Flex';
import { Separator } from '../Separator';

export function DropdownSeparator(): JSX.Element {
  return (
    <Flex padding="4px 0">
      <Separator />
    </Flex>
  );
}
